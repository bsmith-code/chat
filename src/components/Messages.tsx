import React, { useEffect } from 'react'
import io from 'socket.io-client'

const Messages = () => {
  // const socket = io(`${process.env.REACT_APP_API_GATEWAY_URL}:3000`)
  // useEffect(() => {
  //   socket.on('receive message', payload => {
  //     console.log(payload)
  //   })
  // }, [])

  return <div />
}

export default Messages
